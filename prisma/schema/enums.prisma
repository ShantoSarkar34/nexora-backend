enum Role {
    CLIENT
    FREELANCER
    ADMIN
}

enum AuthProvider {
    LOCAL
    GOOGLE
}

enum ExperienceLevel {
    ENTRY
    INTERMEDIATE
    EXPERT
}

enum Availability {
    AVAILABLE
    BUSY
    NOT_AVAILABLE
}

enum CompanySize {
    SOLO
    SMALL
    MEDIUM
    LARGE
}

enum JobCategory {
    WEB_DEVELOPMENT
    MOBILE_DEVELOPMENT
    DESIGN
    WRITING
    MARKETING
    DATA_SCIENCE
    DEVOPS
    OTHER
}

enum BudgetType {
    FIXED
    HOURLY
}

enum JobStatus {
    DRAFT
    OPEN
    IN_PROGRESS
    COMPLETED
    CANCELLED
    CLOSED
}

enum ApplicationStatus {
    PENDING
    SHORTLISTED
    REJECTED
    HIRED
    WITHDRAWN
}

enum ContractStatus {
    PENDING
    ACTIVE
    SUBMITTED
    COMPLETED
    CANCELLED
    DISPUTED
}
